produto_estoque{
	=>codigo(int),
	=>produto(txt),
	=>fornecedor(txt)-fk,
	=>data_entrada(data),
	=>quantidade(int),
	=>validade(data),
	=>preco(float),
	=>
}

=>Tela so com tabela ( cozinha ), nome ( prato ) hora ( pedido ) mesa (numero );
=>Tela caixa, pode encerrar, editar, reabrir ( mesa ); 

=>Bd Mesa(Abri, Fecha, 'boolean')